import React from 'react'

// RC2
function All_Skills() {
    return (
        <div>
            All user's skills grouped by sections
            {/* 
            Sections:
            - education + certificates (together or separately?)
            - work experience
            - hard skills (techincal knowledge)
            - soft skills (communicative, punctual etc)
            - languages
            - other experience (volunteering)
            - what else? 
            */}
        </div>
    )
}

export default All_Skills